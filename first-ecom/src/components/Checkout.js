import React from 'react';

function Checkout() {
	return (
		<div>
			Checkout Page -Display name, $, quantity, total cost. Checkout form
			w/input - name, shipping address, billing address, CC info, submit button.
			SUBMITTING order clears cart + returns to come page
		</div>
	);
}

export default Checkout;
